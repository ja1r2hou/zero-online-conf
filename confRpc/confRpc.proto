syntax = "proto3";

package confRpc;
option go_package="./confRpc";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service ConfRpc {
  rpc Ping(Request) returns(Response);
}
